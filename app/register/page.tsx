"use client"; import {useState} from "react"; import {registerAction} from "@/actions/auth.actions"; export default function Register(){const [msg,setMsg]=useState(""); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await registerAction({email:f.get("email"),password:f.get("password"),username:f.get("username"),displayName:f.get("displayName")});setMsg(r.ok?"Registration successful. Check your email to verify your account.":r.error)} return <main className="wrap" style={{maxWidth:520,paddingTop:70}}><h1>Create CreatorFix Account</h1><form onSubmit={submit} className="card" style={{display:"grid",gap:14}}><input name="displayName" placeholder="Full name" required/><input name="username" placeholder="Username" required/><input name="email" type="email" placeholder="Email" required/><input name="password" type="password" placeholder="Password" required minLength={8}/><button className="btn">Create Account</button><p>{msg}</p></form></main>}