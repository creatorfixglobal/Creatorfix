"use client";import{useState}from"react";import Link from"next/link";import{registerAction}from"@/actions/auth.actions";
export default function Register(){const[msg,setMsg]=useState("");const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setMsg("");const f=new FormData(e.currentTarget);const r=await registerAction({email:f.get("email"),password:f.get("password"),username:f.get("username"),displayName:f.get("displayName")});setLoading(false);setMsg(r.ok?"Registration successful. Check your email inbox to verify your account.":r.error)}return <main className="wrap" style={{maxWidth:520,paddingTop:70}}><h1>Create Account</h1><p className="muted">Create your CreatorFix account in seconds.</p><form onSubmit={submit} className="card" style={{display:"grid",gap:14}}><input name="displayName" placeholder="Full name" required/><input name="username" placeholder="Username" required/><input name="email" type="email" placeholder="Email address" required/><input name="password" type="password" placeholder="Password (minimum 8 characters)" required minLength={8}/><button className="btn" disabled={loading}>{loading?"Creating account...":"Create Account"}</button><p aria-live="polite">{msg}</p></form><p className="muted" style={{marginTop:18}}>Want to work as a specialist? <Link href="/provider/register">Apply as a Provider</Link></p></main>}