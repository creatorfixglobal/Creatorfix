import {NextResponse} from "next/server";import {createServerSupabaseClient} from "@/lib/supabase/server";export async function GET(req:Request){const limit=Math.min(Math.max(Number(new URL(req.url).searchParams.get("limit")||6),1),20);const db=createServerSupabaseClient();const{data,error}=await db.from("short_videos").select("id,caption,playback_url,thumbnail_url,view_count,created_at").eq("status","published").eq("visibility","public").order("created_at",{ascending:false}).limit(limit);if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json(data||[],{headers:{"Cache-Control":"public, s-maxage=30, stale-while-revalidate=60"}})}