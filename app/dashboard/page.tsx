import Link from "next/link";
import { requireRole } from "@/lib/auth/require-role";
import { createServerSupabaseClient } from "@/lib/supabase/server";
const money=(v:number|undefined|null)=>new Intl.NumberFormat("en-BD",{style:"currency",currency:"BDT",maximumFractionDigits:2}).format((v||0)/100);
export default async function DashboardPage(){
 const me=await requireRole(["customer","provider","admin"]); const supabase=createServerSupabaseClient();
 const [{data:profile},{data:wallet},{data:identity},{data:securityDeposit},{data:providerProfile},{count:videoCount},{data:notifications}]=await Promise.all([
  supabase.from("profiles").select("username,display_name,bio,role,status").eq("id",me.id).single(),
  supabase.from("wallets").select("balance,reserved_balance").eq("user_id",me.id).maybeSingle(),
  supabase.from("identity_verifications").select("status").eq("user_id",me.id).order("created_at",{ascending:false}).limit(1).maybeSingle(),
  supabase.from("provider_security_deposits").select("amount,status").eq("user_id",me.id).maybeSingle(),
  supabase.from("provider_profiles").select("verification_status,status").eq("user_id",me.id).maybeSingle(),
  supabase.from("short_videos").select("*",{count:"exact",head:true}).eq("user_id",me.id),
  supabase.from("notifications").select("id,title,body,created_at").eq("user_id",me.id).order("created_at",{ascending:false}).limit(6)
 ]);
 const username=profile?.username||"creator";
 return <main className="dash"><style>{`.dash{min-height:100vh;padding:22px 16px 80px;background:radial-gradient(circle at 90% 0,#193d74,transparent 34%),radial-gradient(circle at 0 75%,#251442,transparent 35%),#070b17}.shell{max-width:1180px;margin:auto}.top{display:flex;justify-content:space-between;gap:16px;align-items:center;flex-wrap:wrap}.hero{margin-top:18px;padding:30px;border:1px solid #2a4566;border-radius:28px;background:linear-gradient(145deg,#111e33,#0b1220)}.name{font-size:clamp(34px,6vw,64px);margin:8px 0}.avatar{width:64px;height:64px;border-radius:22px;display:grid;place-items:center;background:linear-gradient(135deg,#4b90ff,#765bff);font-weight:900;font-size:22px}.profile{display:flex;gap:16px;align-items:center}.grid{display:grid;grid-template-columns:repeat(4,1fr);gap:14px;margin-top:16px}.tile,.panel{padding:22px;border:1px solid #243a57;border-radius:22px;background:linear-gradient(145deg,#101b2d,#0b1422)}.tile strong{display:block;font-size:28px;margin-top:10px}.cols{display:grid;grid-template-columns:1.1fr .9fr;gap:16px;margin-top:16px}.row{padding:14px 0;border-bottom:1px solid #21334d}.row:last-child{border:0}.muted{color:#9eafc6;line-height:1.65}.actions{display:flex;gap:10px;flex-wrap:wrap}.tabs{display:flex;gap:8px;overflow:auto;margin:16px 0}.tab{white-space:nowrap;padding:10px 15px;border:1px solid #29405d;border-radius:999px;color:#b7c5d8}.tab.active{background:#e2edff;color:#091321;font-weight:800}.notice{padding:15px 18px;border:1px solid #29415f;border-radius:18px;background:#101a29;color:#c9d7e9}.progress{height:7px;background:#17283d;border-radius:999px;margin-top:8px;overflow:hidden}.progress i{display:block;width:50%;height:100%;background:linear-gradient(90deg,#54a5ff,#765bff)}@media(max-width:850px){.grid{grid-template-columns:repeat(2,1fr)}.cols{grid-template-columns:1fr}}@media(max-width:480px){.grid{grid-template-columns:1fr}.hero{padding:22px}}`}</style><div className="shell">
 <div className="top"><Link href="/">← CreatorFix</Link><div className="actions"><Link className="btn" href={"/u/"+encodeURIComponent(username)}>Public Profile</Link><Link className="btn" href="/wallet">Wallet</Link><Link className="btn" href="/videos/upload">Upload Short</Link></div></div>
 <section className="hero"><div className="profile"><div className="avatar">{(profile?.display_name||me.displayName||"C").slice(0,2).toUpperCase()}</div><div><span className="muted">ACCOUNT CENTER</span><h1 className="name">{profile?.display_name||me.displayName}</h1><p className="muted">@{username} · {me.role.toUpperCase()} · {profile?.status||me.status}</p></div></div><p className="muted">{profile?.bio||"Manage your CreatorFix activity, verification, wallet and creator media from one secure place."}</p></section>
 <nav className="tabs"><span className="tab active">Overview</span><Link className="tab" href="/wallet">Wallet</Link><Link className="tab" href={"/u/"+encodeURIComponent(username)}>Profile</Link><Link className="tab" href="/videos">Shorts</Link><Link className="tab" href="/provider/register">Provider</Link><Link className="tab" href="/verify">Verification</Link></nav>
 <div className="notice"><b>Creator profile progress</b><div className="progress"><i/></div><span className="muted" style={{fontSize:13}}>Complete identity and provider requirements to unlock trusted marketplace features.</span></div>
 <section className="grid"><div className="tile"><span className="muted">AVAILABLE BALANCE</span><strong>{money(wallet?.balance)}</strong></div><div className="tile"><span className="muted">RESERVED / HELD</span><strong>{money(wallet?.reserved_balance)}</strong></div><div className="tile"><span className="muted">SECURITY DEPOSIT</span><strong>{securityDeposit?money(securityDeposit.amount):"BDT 0.00"}</strong><span className="muted">{securityDeposit?.status||"No provider hold"}</span></div><div className="tile"><span className="muted">SHORT VIDEOS</span><strong>{videoCount||0}</strong></div></section>
 <section className="cols"><div className="panel"><h2>Verification & Provider Status</h2><div className="row"><b>Identity</b><p className="muted">{identity?.status||"Not submitted"}</p></div><div className="row"><b>Provider</b><p className="muted">{providerProfile?.status||"Customer account"} {providerProfile?.verification_status?"· "+providerProfile.verification_status:""}</p></div><div className="row"><b>BDT 1,000 security rule</b><p className="muted">{securityDeposit?"Security hold status: "+securityDeposit.status+". Releasing the hold can remove provider working eligibility under marketplace rules.":"Required before provider activation."}</p></div><div className="actions"><Link className="btn" href="/provider/register">Provider Center</Link><Link className="btn" href="/verify">Verify Identity</Link></div></div>
 <div className="panel"><h2>Recent Updates</h2>{!notifications?.length?<p className="muted">No notifications yet.</p>:notifications.map(n=><div className="row" key={n.id}><b>{n.title}</b><p className="muted">{n.body}</p></div>)}</div></section>
 </div></main>
}