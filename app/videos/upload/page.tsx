"use client";import {useState} from "react";import Link from "next/link";
export default function UploadShortVideo(){const [file,setFile]=useState<File|null>(null),[caption,setCaption]=useState(""),[busy,setBusy]=useState(false),[message,setMessage]=useState("");async function upload(){if(!file){setMessage("Choose a video first.");return}setBusy(true);setMessage("");try{const sign=await fetch("/api/cloudinary/sign",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({resourceType:"video"})});const signed=await sign.json();if(!sign.ok)throw new Error(signed.error||"Cloudinary is not configured.");const form=new FormData();form.append("file",file);form.append("api_key",signed.apiKey);form.append("timestamp",String(signed.timestamp));form.append("signature",signed.signature);form.append("folder",signed.folder);const cloud=await fetch(`https://api.cloudinary.com/v1_1/${signed.cloudName}/video/upload`,{method:"POST",body:form});const asset=await cloud.json();if(!cloud.ok)throw new Error(asset.error?.message||"Video upload failed.");const save=await fetch("/api/videos",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({playbackUrl:asset.secure_url,thumbnailUrl:null,cloudinaryPublicId:asset.public_id,caption})});const result=await save.json();if(!save.ok)throw new Error(result.error||"Could not save video.");window.location.href="/videos";}catch(e:any){setMessage(e.message||"Upload failed.");}finally{setBusy(false)}}return <main className="up"><style>{`.up{min-height:100vh;padding:32px 18px;background:radial-gradient(circle at 80% 0,#1b4278,transparent 40%),#070b16}.box{max-width:680px;margin:30px auto;padding:30px;border:1px solid #294563;border-radius:28px;background:#0c1727}.muted{color:#9eafc6;line-height:1.65}.field{display:grid;gap:8px;margin:18px 0}.field input,.field textarea{background:#08111f;color:#fff;border:1px solid #29415e;border-radius:14px;padding:14px}`}</style><Link href="/videos">← Back to Shorts</Link><section className="box"><span className="muted">CREATORFIX SHORTS</span><h1>Upload a short video</h1><p className="muted">Video files upload directly to Cloudinary using a server-generated signature. Private identity documents never use this media pipeline.</p><label className="field"><b>Video</b><input type="file" accept="video/*" onChange={e=>setFile(e.target.files?.[0]||null)}/></label><label className="field"><b>Caption</b><textarea rows={4} maxLength={500} value={caption} onChange={e=>setCaption(e.target.value)} placeholder="Tell the community about this short..."/></label><button className="btn" disabled={busy} onClick={upload}>{busy?"Uploading...":"Publish Short Video"}</button>{message&&<p className="muted" style={{color:"#ffb8c5"}}>{message}</p>}</section></main>}