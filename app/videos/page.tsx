import Link from "next/link";import { createServerSupabaseClient } from "@/lib/supabase/server";import ShortsFeed from "@/components/shorts-feed";
export default async function ShortVideosPage(){const supabase=createServerSupabaseClient();const {data:videos}=await supabase.from("short_videos").select("id,user_id,caption,playback_url,thumbnail_url,view_count,like_count,comment_count,created_at").eq("status","published").eq("visibility","public").order("created_at",{ascending:false}).limit(50);const ids=[...new Set((videos||[]).map(v=>v.user_id))];const {data:creators}=ids.length?await supabase.from("creator_profiles_public").select("id,username,display_name,avatar_url").in("id",ids):{data:[] as any[]};return <ShortsFeed videos={videos||[]} creators={creators||[]}/>}