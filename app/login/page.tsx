"use client"; import {useState} from "react"; import {loginAction} from "@/actions/auth.actions"; export default function Login(){const [msg,setMsg]=useState(""); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await loginAction({email:f.get("email"),password:f.get("password")});setMsg(r.ok?"Login successful. Redirecting...":r.error);if(r.ok)location.href="/dashboard"} return <main className="wrap" style={{maxWidth:520,paddingTop:70}}><h1>Login</h1><form onSubmit={submit} className="card" style={{display:"grid",gap:14}}><input name="email" type="email" placeholder="Email" required/><input name="password" type="password" placeholder="Password" required/><button className="btn">Login</button><p>{msg}</p></form></main>}