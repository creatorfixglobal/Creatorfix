import {requireRole} from "@/lib/auth/require-role"; export default async function AdminVerification(){await requireRole(["admin"]);return <main className="wrap" style={{paddingTop:50}}><h1>Identity Verification Review</h1><div className="card"><p>Admin review queue UI is protected. Next backend batch will load pending verification records and use signed URLs only after authorization.</p></div></main>}