import {requireRole} from "@/lib/auth/require-role"; import Link from "next/link"; export default async function Admin(){await requireRole(["admin"]);return <main className="wrap" style={{paddingTop:50}}><h1>Admin Control Center</h1><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(220px,1fr))",gap:16}}>{[["Identity Reviews","/admin/verification"],["Users","/admin/users"],["Provider Applications","/admin/providers"],["Marketplace","/admin/marketplace"]].map(([a,b])=><Link className="card" href={b} key={a}><h3>{a}</h3><p className="muted">Administrative controls</p></Link>)}</div></main>}