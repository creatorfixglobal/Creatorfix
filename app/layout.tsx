import type { Metadata } from "next";import "./globals.css";import GlobalAccountBar from "@/components/global-account-bar";export const metadata:Metadata={title:"CreatorFix",description:"Verified creator services marketplace"};export default async function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body><GlobalAccountBar/>{children}</body></html>}