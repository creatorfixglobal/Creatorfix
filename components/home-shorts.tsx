"use client";import {useEffect,useState} from "react";import Link from "next/link";export default function HomeShorts(){const [videos,setVideos]=useState<any[]>([]);useEffect(()=>{fetch("/api/videos/feed?limit=6").then(r=>r.ok?r.json():[]).then(setVideos).catch(()=>{})},[]);return <section className="section"><div style={{display:"flex",justifyContent:"space-between",alignItems:"end",gap:16,flexWrap:"wrap"}}><div><span className="tag">CREATORFIX SHORTS</span><h2>Watch what creators are sharing</h2><p className="muted-home">A vertical short-video experience built into the CreatorFix ecosystem.</p></div><Link className="secondary" href="/videos">Open Shorts →</Link></div>{videos.length?<div className="short-rail">{videos.map(v=><Link className="short-card" href="/videos" key={v.id}><video src={v.playback_url} poster={v.thumbnail_url||undefined} muted playsInline preload="metadata"/><div><b>{v.caption||"CreatorFix Short"}</b><p>{v.view_count||0} views</p></div></Link>)}</div>:<div className="short-empty">No shorts yet. <Link href="/videos/upload">Upload the first short →</Link></div>}</section>}