"use client";import {useState} from "react";import {useRouter} from "next/navigation";import {adminApproveDepositAction,adminRejectDepositAction,adminReviewWalletWithdrawalAction} from "@/actions/wallet.actions";function Btn({run,label}:{run:()=>Promise<any>;label:string}){const r=useRouter(),[b,setB]=useState(false),[m,setM]=useState("");return <span style={{display:"inline-flex",gap:6,alignItems:"center"}}><button className="btn" disabled={b} onClick={async()=>{setB(true);const x=await run();setB(false);setM(x.ok?"Done":x.error||"Failed");if(x.ok)r.refresh()}}>{label}</button>{m&&<small>{m}</small>}</span>}export function DepositControls({id}:{id:string}){return <div style={{display:"flex",gap:8,flexWrap:"wrap"}}><Btn label="Approve & Credit" run={()=>adminApproveDepositAction(id)}/><Btn label="Reject" run={()=>adminRejectDepositAction(id)}/></div>}export function WithdrawalControls({id}:{id:string}){return <div style={{display:"flex",gap:8,flexWrap:"wrap"}}><Btn label="Mark Paid" run={()=>adminReviewWalletWithdrawalAction(id,"paid")}/><Btn label="Reject & Release" run={()=>adminReviewWalletWithdrawalAction(id,"rejected","Rejected by administrator")}/></div>}