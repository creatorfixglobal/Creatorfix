# CreatorFix — Identity Verification (NID + Live Face) Architecture

This is a **mandatory prerequisite** for core marketplace functionality, not an optional profile enhancement. It supersedes/extends sections 4 (schema), 5 (permission matrix), 9 (RLS), 11–12 (sto[...]